import { changePasswordAction } from "@/app/account/actions";
import { requireSessionUser } from "@/server/auth/current-user";
export const dynamic = "force-dynamic";
export default async function PasswordPage({ searchParams }: { searchParams: Promise<{ error?: string }> }) {
  const [user, params] = await Promise.all([requireSessionUser(), searchParams]);
  return <main className="flex min-h-screen items-center justify-center bg-slate-100 px-6 py-12"><section className="w-full max-w-lg rounded-3xl border border-slate-200 bg-white p-7 shadow-xl"><p className="text-sm font-bold uppercase tracking-[0.2em] text-amber-700">Account security</p><h1 className="mt-2 text-3xl font-bold text-slate-950">Change password</h1><p className="mt-2 text-slate-600">Signed in as {user.name}. Passwords must contain at least 12 characters.</p>{user.mustChangePassword ? <p className="mt-5 rounded-xl bg-amber-50 px-4 py-3 text-sm font-semibold text-amber-900">Change your temporary password before opening claim files.</p> : null}{params.error ? <p className="mt-5 rounded-xl bg-rose-50 px-4 py-3 text-sm font-semibold text-rose-800">{params.error}</p> : null}<form action={changePasswordAction} className="mt-6 space-y-4"><label className="block"><span className="text-sm font-semibold">Current password</span><input required type="password" name="currentPassword" autoComplete="current-password" className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3"/></label><label className="block"><span className="text-sm font-semibold">New password</span><input required minLength={12} type="password" name="newPassword" autoComplete="new-password" className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3"/></label><label className="block"><span className="text-sm font-semibold">Confirm new password</span><input required minLength={12} type="password" name="confirmPassword" autoComplete="new-password" className="mt-2 w-full rounded-xl border border-slate-300 px-4 py-3"/></label><button className="w-full rounded-xl bg-slate-950 px-5 py-3 font-bold text-white">Update password</button></form></section></main>;
}
